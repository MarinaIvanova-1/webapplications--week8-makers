<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset=utf-8>
    <title>Greeter app</title>
  </head>
  <body>
    <p>
    <% special = "?<>',?[]}{=)(*&^%$#`~{}" %>
    <% regex = /[#{special.gsub(/./){|char| "\\#{char}"}}]/ %>
      <% if @name =~ regex %>
        This doesn't look like a name
      <% else %>
        Hi <%= @name %>!
      <% end %>
    </p>
  </body>
</html>